let change_timer_by = 0
basic.forever(function () {
    let timer = 0
    basic.showIcon(IconNames.Heart)
    basic.pause(100)
    change_timer_by += 1
    if (timer) {
    	
    } else {
    	
    }
})
basic.forever(function () {
	
})
